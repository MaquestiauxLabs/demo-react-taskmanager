model User {
    id         String   @id @unique @default(cuid())
    email      String   @unique
    givenName  String
    familyName String
    avatarUrl  String?
    createdAt  DateTime @default(now())
    updatedAt  DateTime @updatedAt
}
